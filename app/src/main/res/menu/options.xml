<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <item android:id="@+id/info" app:showAsAction="ifRoom"
        android:iconTint="@android:color/white"
          android:title="@string/info" android:icon="@drawable/icon_info"/>
    <item
          android:id="@+id/addArticle" android:icon="@drawable/add_button"
          android:title="@string/add_article" app:showAsAction="always"
          android:iconTint="@android:color/white"
     />
</menu>